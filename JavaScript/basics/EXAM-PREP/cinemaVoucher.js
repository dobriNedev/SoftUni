function solve(input) {
    let index = 0;
    let value = Number(input[index++]);
    let command = input[index++];

    let isMoneyLeft = true;

    while (command !== "End") {
        




        command = input[index++];
    }
}
solve(["1500",
"Avengers: Endgame",
"Bohemian Rhapsody",
"Deadpool 2",
"End"]);